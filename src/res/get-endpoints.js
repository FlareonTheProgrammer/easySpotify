/**
 * @name GME
 * * Get Method Endpoints
 * (All valid Spotify endpoints that take a GET request)
 * 
 * @type {Object}
 */
const gme = {
    artists: "/v1/artists",
    albums: "/v1/albums",
    search: "/v1/search"
}

module.exports = {
    gme: gme
}