/**
 * @name GME
 * * Get Method Endpoints
 * (All valid Spotify endpoints that take a GET request)
 * 
 * @type {Object}
 */
const gme = {
    artists: "/v1/artists",
    albums: "/v1/albums"
}

module.exports = {
    gme: gme
}